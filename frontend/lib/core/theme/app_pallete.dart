import 'package:flutter/material.dart';

class AppPallete {
  
  static const Color backgroundColor = Colors.white;
  static const Color cardBackground = Color(0xFFF2F2F2); 

  static const Color gradient1 = Colors.orange;
  static const Color gradient2 = Colors.white;
  static const Color gradient3 = Colors.green;

  static const Color primaryColor = Colors.orange; // Sign up button
  static const Color accentColor = Colors.green;   // Optional highlights

  static const Color borderColor = Color(0xFFE0E0E0);
  static const Color textColor = Colors.black87;

  static const Color whiteColor = Colors.white;
  static const Color greyColor = Colors.grey;
  static const Color errorColor = Colors.redAccent;
  static const Color transparentColor = Colors.transparent;
}
